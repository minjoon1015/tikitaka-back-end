package FutureCraft.tikitaka.back_end.common;

public enum ResponseCode {
    DE, // database_error
    SC, // success
    BR, // bad_request
    DUI, // duplicate_id
    NEI, // not_exists_id
    NEP, // not_equals_password
}
